# Path to the private listings database (never committed). The schema is documented in
# src/appraisenet/data.py; without it, every command falls back to the synthetic corpus.
APPRAISENET_DB=data/listings.db
# MLflow tracking (defaults to sqlite:///reports/mlflow.db)
# MLFLOW_TRACKING_URI=
