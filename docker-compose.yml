services:
  api:
    build: .
    ports: ["8080:8080"]
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
    environment:
      - APPRAISENET_DB=/app/data/listings.db
  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.12.1
    command: mlflow server --backend-store-uri sqlite:///mlflow/mlflow.db --host 0.0.0.0 --port 5000
    ports: ["5000:5000"]
    volumes: ["./reports:/mlflow"]
